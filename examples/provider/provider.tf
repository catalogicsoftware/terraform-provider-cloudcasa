# Configuration-based authentication
provider "cloudcasa" {
  apikey = "Your API Key here"
  # Optional: customize the CloudCasa URL 
  # cloudcasa_url = "https://home.cloudcasa.io"
  # Optional: allow insecure TLS connections for Selfhosted CloudCasa servers with self-signed certificates
  # insecure_tls = true
}
